Datasets and data resources used by KryptonxWatch.

- `bakeoff/`: HawkWatch's 12 UCF-Crime clips plus UCF test clips (timed and normal) for the [model bake-off](../model/openrouter-bakeoff/README.md). Build it with `python3 model/openrouter-bakeoff/fetch_data.py`.
